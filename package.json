{
  "name": "data-flow",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "generate:parser": "node scripts/generateDslParser.mjs",
    "predev": "npm run generate:parser",
    "dev": "vite",
    "prebuild": "npm run generate:parser",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/dagre": "^0.7.53",
    "peggy": "^5.0.6",
    "typescript": "^6.0.3",
    "vite": "^8.0.14"
  },
  "dependencies": {
    "@codemirror/state": "^6.5.4",
    "@panzoom/panzoom": "^4.6.1",
    "codemirror": "^6.0.2",
    "dagre": "^0.8.5"
  }
}
